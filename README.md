# My Dotfile

## Install

```sh
curl -sL https://raw.githubusercontent.com/0711kps/dotfile/refs/heads/main/init.sh | bash
```

## Some Notes

- for some driver reasons
  + CJS forge 14V must install debian `without` wired network connection
  + vivobook s5406sa must install debian `with` wired connection
